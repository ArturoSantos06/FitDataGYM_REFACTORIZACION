import { Calendar, Clock, RefreshCw, User, XCircle } from 'lucide-react';

function TarjetaCitaNutricion({ cita, alReprogramar, alCancelar }) {
  const cancelada = cita.estado === 'cancelada';
  return <div className={`bg-slate-900 rounded-xl p-6 border transition-all ${cancelada ? 'border-red-500/20 opacity-75' : 'border-slate-700 hover:border-emerald-500/50 hover:shadow-lg hover:shadow-emerald-900/20'}`}><div className="flex flex-col md:flex-row justify-between md:items-center gap-4"><div className="space-y-3 flex-1"><div className="flex items-center justify-between md:justify-start gap-4"><h3 className="text-xl font-bold text-white capitalize">{cita.title || 'Cita Nutricional'}</h3><span className={`px-3 py-1 rounded-full text-xs font-bold ${cancelada ? 'bg-red-900/30 text-red-400 border border-red-500/50' : 'bg-emerald-900/30 text-emerald-400 border border-emerald-500/50'}`}>{cancelada ? 'Cancelada' : 'Activa'}</span></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-2 text-sm text-slate-300"><div className="flex items-center gap-2"><Calendar size={16} className="text-emerald-500" />{cita.fecha}</div><div className="flex items-center gap-2"><Clock size={16} className="text-emerald-500" />{cita.horaInicio} - {cita.horaFin}</div><div className="flex items-center gap-2 sm:col-span-2"><User size={16} className="text-emerald-500" />Nutriólogo asignado (Contactar vía mensaje)</div></div></div>{!cancelada && <div className="flex md:flex-col gap-2 md:min-w-[140px] pt-4 md:pt-0 border-t md:border-t-0 md:border-l border-slate-700 md:pl-4 mt-2 md:mt-0"><button onClick={() => alReprogramar(cita)} className="flex-1 flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg bg-slate-800 hover:bg-slate-700 text-emerald-400 font-semibold text-sm transition-colors border border-slate-600"><RefreshCw size={16} />Cambiar</button><button onClick={() => alCancelar(cita.id)} className="flex-1 flex items-center justify-center gap-2 px-4 py-2.5 rounded-lg bg-red-900/20 hover:bg-red-600 text-red-400 font-semibold text-sm transition-all border border-red-900/50"><XCircle size={16} />Cancelar</button></div>}</div></div>;
}

export default TarjetaCitaNutricion;
